import { Typography } from '@mui/material';
import React from 'react';

const Order = () => {
  //  const{_id,name,description,price,address,mobile,email,license,payment}=props.order
    return (
        <div>
         <Typography variant="h5" component="div">
   <h2>This is Order</h2>
        </Typography>
        </div>
    );
};

export default Order;